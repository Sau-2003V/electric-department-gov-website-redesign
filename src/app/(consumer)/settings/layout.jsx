export const metadata = {
  title: "Account Settings · Vidhyut Electricity Portal",
  description:
    "Manage your consumer profile, update contact information, theme preferences, and security settings.",
};

export default function SettingsLayout({ children }) {
  return children;
}
