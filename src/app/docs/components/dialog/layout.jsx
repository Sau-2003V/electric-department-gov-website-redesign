export const metadata = {
  title: "Dialog Component · Design System · Vidhyut Portal",
  description:
    "Accessible modal dialogs, drawer overlays, confirm alerts, and spring animations with focus traps.",
};

export default function DialogLayout({ children }) {
  return children;
}
