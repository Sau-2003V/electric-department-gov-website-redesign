export const metadata = {
  title: "Button Component · Design System · Vidhyut Portal",
  description:
    "Action buttons with full variant hierarchy, size ladders, icon slots, loading states, and tactile interactions.",
};

export default function ButtonLayout({ children }) {
  return children;
}
