export const metadata = {
  title: "Badge Component · Design System · Vidhyut Portal",
  description:
    "Status tags, category pills, counters, indicators, and pastel ramps conforming to design system tokens.",
};

export default function BadgeLayout({ children }) {
  return children;
}
