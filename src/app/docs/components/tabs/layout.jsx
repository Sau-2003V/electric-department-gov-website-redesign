export const metadata = {
  title: "Tabs & Nav-Pill Component · Design System · Vidhyut Portal",
  description:
    "Segmented controls, nav-pill switchers, and underline tab bars with fluid spring transitions.",
};

export default function TabsLayout({ children }) {
  return children;
}
